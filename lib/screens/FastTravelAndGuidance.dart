import 'package:flutter/material.dart';

class FastTravelAndGuidance extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.deepOrange,
    );
  }
}